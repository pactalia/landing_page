---
---
<section class="py-24" id="waitlist">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl sm:text-4xl font-inter font-bold mb-4">
        Únete a la Lista de Espera
      </h2>
      <p class="text-lg text-gray-600 dark:text-gray-300 mb-8">
        Sé el primero en acceder cuando lancemos. Sin cargo, sin compromiso.
      </p>
      <form class="space-y-4" id="waitlist-form" name="waitlist-form" method="POST" data-netlify-recaptcha="true" netlify-honeypot="bot-field" data-netlify="true">
        <div>
          <p class="hidden">
            <label>
              Don’t fill this out if you’re human: <input name="bot-field" />
            </label>
          </p>
        </div>
        <div>
          <input
            type="text"
            id="name"
            name="name"
            class="w-full px-4 py-3 rounded-lg bg-gray-50 dark:bg-gray-800 border border-gray-300 dark:border-gray-700 focus:ring-2 focus:ring-primary focus:border-transparent"
            placeholder="Nombre completo"
            required
          />
        </div>
        <div>
          <input
            type="email"
            id="email"
            name="email"
            class="w-full px-4 py-3 rounded-lg bg-gray-50 dark:bg-gray-800 border border-gray-300 dark:border-gray-700 focus:ring-2 focus:ring-primary focus:border-transparent"
            placeholder="Tu correo electrónico"
            required
          />
        </div>
        <div>
          <input
            type="text"
            id="city"
            name="city"
            class="w-full px-4 py-3 rounded-lg bg-gray-50 dark:bg-gray-800 border border-gray-300 dark:border-gray-700 focus:ring-2 focus:ring-primary focus:border-transparent"
            placeholder="Ciudad"
            required
          />
        </div>
        <div>
          <select
            id="role"
            name="role"
            class="w-full px-4 py-3 rounded-lg bg-gray-50 dark:bg-gray-800 border border-gray-300 dark:border-gray-700 focus:ring-2 focus:ring-primary focus:border-transparent"
            required
          >
            <option value="">Selecciona tu rol</option>
            <option value="buyer">Comprador</option>
            <option value="seller">Vendedor</option>
            <option value="both">Ambos</option>
          </select>
        </div>
        <div class="text-left">
          <p class="mb-2 text-gray-700 dark:text-gray-300">Selecciona los marketplaces que usas:</p>
          <div class="space-y-2">
            <label class="flex items-center">
              <input type="checkbox" name="marketplace" value="facebook" class="w-4 h-4 text-primary rounded border-gray-300 focus:ring-primary">
              <span class="ml-2">Facebook Marketplace</span>
            </label>
            <label class="flex items-center">
              <input type="checkbox" name="marketplace" value="instagram" class="w-4 h-4 text-primary rounded border-gray-300 focus:ring-primary">
              <span class="ml-2">Instagram</span>
            </label>
            <label class="flex items-center">
              <input type="checkbox" name="marketplace" value="telegram" class="w-4 h-4 text-primary rounded border-gray-300 focus:ring-primary">
              <span class="ml-2">Telegram</span>
            </label>
            <label class="flex items-center">
              <input type="checkbox" name="marketplace" value="craiglist" class="w-4 h-4 text-primary rounded border-gray-300 focus:ring-primary">
              <span class="ml-2">Craiglist</span>
            </label>
            <label class="flex items-center">
              <input type="checkbox" name="marketplace" value="other" class="w-4 h-4 text-primary rounded border-gray-300 focus:ring-primary">
              <span class="ml-2">Otro</span>
            </label>
          </div>
        </div>

        <div data-netlify-recaptcha="true"></div>

        <button
          type="submit"
          class="w-full px-8 py-3 text-base font-medium text-white bg-primary hover:bg-primary/90 rounded-lg transition-colors duration-300"
        >
          Unirme a la lista de espera
        </button>
      </form>
    </div>
  </div>
</section>

<!-- <script>
  const form = document.getElementById('waitlist-form');
  
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(e.target as HTMLFormElement);
    const email = formData.get('email');
    const role = formData.get('role');
    
    try {
      // Here you would typically send the data to your backend
      console.log('Form submitted:', { email, role });
      alert('¡Gracias por unirte! Te contactaremos pronto.');
      (e.target as HTMLFormElement).reset();
    } catch (error) {
      console.error('Error:', error);
      alert('Hubo un error. Por favor intenta de nuevo.');
    }
  });
</script> -->